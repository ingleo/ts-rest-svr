# Typescript REST server from scratch

1. `npm --init`
2. `npm install typescript tslint`
3. `./node_modules/.bin/tsc --init`
4. `./node_modules/.bin/tlint --init`
5. install other dependencies

### For build
`npm run tsc-build`
